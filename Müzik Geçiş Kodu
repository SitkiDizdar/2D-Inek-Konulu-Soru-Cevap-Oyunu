using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MüzikGeçiş : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
         DontDestroyOnLoad(this.gameObject);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

Yapmanız gereken ilk sahne ekranında bu kodu kullanmak devamında kendisi müzik bitene kadar çalışacaktır. 
